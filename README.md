# pipeline-eng-change-powerbi
Python + SQL pipeline that turns a messy system export into clean tables for Power BI: date parsing, deduplication, business rules and data quality checks. Runs on synthetic data.
